<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.magnum.videoup.client"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/heart_beat"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <receiver android:name=".AlarmReceiver" />

        <activity
            android:name="org.magnum.symptom.client.LoginScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.magnum.symptom.client.VideoListActivity"
            android:label="@string/title_activity_video_list" />
        <activity
            android:name="org.magnum.symptom.client.PatientActivity"
            android:label="@string/title_activity_patient" />
        <activity
            android:name="org.magnum.symptom.client.CheckInActivity"
            android:label="@string/title_activity_check_in" />
        <activity
            android:name="org.magnum.symptom.reminder.ReminderActivity"
            android:label="@string/title_activity_reminder" />
        <!-- created by ADT ext plugin at 2014-11-06 00:43:46 -->
        <activity android:name="org.magnum.symptom.client.DoctorActivity" />
        <activity
            android:name="org.magnum.symptom.client.DoctorActivity"
            android:label="@string/title_activity_doctor" >
        </activity>
    </application>

</manifest>
